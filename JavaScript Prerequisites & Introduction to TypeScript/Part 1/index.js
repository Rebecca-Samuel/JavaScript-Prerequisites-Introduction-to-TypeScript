import formatCurrency, { add, capitalize, APP_VERSION } from "./utils.js"

console.log(add(10, 20))             // 30
console.log(capitalize("frontend"))  // Frontend
console.log(APP_VERSION)             // 1.0.0
console.log(formatCurrency(45000))   // ₦45,000.00